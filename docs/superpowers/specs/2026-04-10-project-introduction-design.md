# 多智能体VLN导航系统项目介绍设计文档

## 概述

本文档为多智能体视觉-语言导航系统项目介绍的设计文档，包含Markdown文档结构和PPT大纲，用于答辩/汇报展示。

**用途**：项目展示/答辩汇报
**受众**：混合受众（导师+同学），兼顾深度和清晰度
**内容比例**：算法结构、应急数据集、模型微调三部分均衡（约各35%、35%、20%）
**呈现形式**：Markdown文档 + PPT大纲
**实验结果**：暂不包含，后续补充

---

## 一、Markdown文档结构

### 1. 项目概述

```markdown
# 多智能体视觉-语言导航系统

## 1. 项目概述

本系统是一个**分层多智能体协作导航框架**，基于Habitat模拟环境，实现智能体根据自然语言指令在3D场景中自主导航。系统核心由三部分组成：

- **算法结构**：分层多智能体架构，根据任务复杂度自适应调度
- **应急数据集**：基于R2R构建的动态障碍应急导航数据
- **模型微调**：QLoRA高效微调，实现应急场景快速适配

（整体架构图预留位置）
```

---

### 2. 算法结构

```markdown
## 2. 算法结构

### 2.1 分层架构设计

系统采用**弱层-强层双层架构**，根据任务复杂度自动切换：

| 层级 | 处理能力 | 适用任务 | 特点 |
|------|----------|----------|------|
| **弱层** | 本地小模型 | Type-0（单步指令） | 快速响应，低资源消耗 |
| **强层** | 多智能体协作 | Type-1~Type-4 | LLM推理，复杂决策 |

### 2.2 任务复杂度分类

系统将导航任务分为5个复杂度等级：

| 类型 | 描述 | 智能体组合 | 推理策略 |
|------|------|------------|----------|
| Type-0 | 单步指令（如"前进"） | 无 | 无 |
| Type-1 | 走廊导航 | Perception + Decision | ReAct |
| Type-2 | 物体查找 | Perception + Trajectory + Decision | ReAct + CoT |
| Type-3 | 跨房间导航 | 全部4个智能体 | CoT + Reflection |
| Type-4 | 模糊/复杂场景 | 全部4个智能体 | CoT + Debate + Reflection |

**分类流程**：规则分类器（快速）→ 置信度<0.9 → LLM分类器（精准）

### 2.3 Supernet编排层

Supernet是系统核心编排层，负责：
- 任务类型识别
- 智能体池管理
- 策略链构建
- 执行流程协调

**执行流程**：
```
输入指令 → TaskClassifier分类 → Supernet选择配置 
         → 激活对应智能体 → 执行策略链 → 输出导航动作
```

### 2.4 四类专用智能体

| 智能体 | 职责 | 输入 | 输出 |
|--------|------|------|------|
| **InstructionAgent** | 指令理解、子任务分解 | 自然语言指令 | subtasks, completion_condition |
| **PerceptionAgent** | 环境感知、场景理解 | RGB + Depth图像 | room_type, objects, nav_hint, danger_info |
| **TrajectoryAgent** | 轨迹分析、历史记录 | 位置历史、动作序列 | distance, heading, stuck_state, history_summary |
| **DecisionAgent** | 最终决策输出 | 所有智能体信息 | action_sequence, reasoning, confidence |

### 2.5 四种推理策略

| 策略 | 适用场景 | 流程 | 响应时间 |
|------|----------|------|----------|
| **ReAct** | 简单任务 | 观察→思考→行动循环 | 2-3秒 |
| **CoT** | 中等复杂度 | 链式推理分解问题 | 3-5秒 |
| **Debate** | 存在冲突/不确定 | 多智能体讨论仲裁 | 10-15秒 |
| **Reflection** | 需要复盘调整 | 执行→反思→修正 | 5-8秒 |

### 2.6 应急响应机制

检测到应急信号时，系统切换至**快速路径模式**：
- 跳过LLM推理
- 直接调用PathReplanner算法
- 响应时间<1秒
```

---

### 3. 应急数据集

```markdown
## 3. 应急数据集

### 3.1 应急场景定义

传统VLN数据集（如R2R）仅包含静态环境导航指令。本项目扩展了**动态障碍应急场景**：

| 场景类型 | 描述 | 典型指令示例 |
|----------|------|--------------|
| **blocked_path** | 路径阻断 | "Walk forward, the path is blocked, turn left to reach the exit." |
| **dynamic_obstacle** | 动态障碍物 | "Proceed straight, obstacle appeared, find alternative route." |
| **emergency_evacuation** | 紧急撤离 | "Emergency: Navigate to the exit, obstacle detected, quickly turn right." |

### 3.2 数据集构建流程

基于R2R数据集改造生成应急指令：

**改造流程**：
1. 加载原始R2R episode（含场景、起点、终点、指令）
2. 选择应急场景类型
3. 应用指令模板生成应急指令
4. 配置障碍物触发时机（trigger_step）与位置
5. 评估指令难度等级

### 3.3 应急指令模板设计

设计了三类应急指令模板：

```
模板A（blocked_path）：
"{normal_action}, the path is blocked, {reroute_action} to reach {goal}."

模板B（dynamic_obstacle）：
"{normal_action}, suddenly blocked, quickly {escape_action}."

模板C（emergency_evacuation）：
"Emergency: {normal_action}, route blocked, {emergency_action}."
```

模板变量填充示例：
- normal_action: "Walk forward", "Go straight"
- reroute_action: "turn left and find another way"
- goal: "the exit", "the destination"

### 3.4 数据集构成

| 数据子集 | 样本数量 | 用途 |
|----------|----------|------|
| 训练集 | 400 | QLoRA微调训练 |
| 验证集 | 80 | 微调过程验证 |
| 测试集 | 80 | 最终评估（不参与训练） |

**样本结构**：
```json
{
  "episode_id": 1,
  "scene_id": "mp3d_abc123",
  "original_instruction": "Walk down the stairs...",
  "emergency_instruction": "Walk forward, the path is blocked...",
  "obstacle_config": {
    "type": "blocked_path",
    "trigger_step": 15,
    "position": [5.2, 0.0, 3.1],
    "radius": 1.5
  },
  "difficulty": "medium"
}
```

### 3.5 难度评估标准

根据指令复杂度自动评估难度：

| 障度 | 评估规则 | 占比 |
|------|----------|------|
| **easy** | 无转弯指令，单步动作 | ~25% |
| **medium** | 1-2次转弯，复合动作 | ~50% |
| **hard** | 3次以上转弯，复杂路径 | ~25% |
```

---

### 4. 模型微调

```markdown
## 4. 模型微调

### 4.1 微调方法选择

采用**QLoRA（Quantized LoRA）**进行高效微调，具有以下优势：

| 优势 | 说明 |
|------|------|
| **低显存需求** | 4-bit量化，7B模型仅需~15GB显存 |
| **训练速度快** | 仅训练LoRA适配器参数（约0.3%原始参数） |
| **模型可复用** | 一个基座模型可加载多个LoRA适配器 |

### 4.2 基础模型配置

| 配置项 | 设置 |
|--------|------|
| 基础模型 | Qwen3.5-9B |
| 量化方式 | 4-bit NF4量化 |
| 计算精度 | BF16 |

### 4.3 LoRA参数配置

| 参数 | 值 | 说明 |
|------|-----|------|
| LoRA秩 | 16 | 适配器维度 |
| LoRA alpha | 32 | 缩放因子 |
| Dropout率 | 0.05 | 防止过拟合 |
| 目标模块 | in_proj_qkv, out_proj, gate_proj, up_proj, down_proj | 线性注意力层 + MLP层 |

### 4.4 训练配置

| 参数 | 值 |
|------|-----|
| 训练轮数 | 2 epochs |
| Batch size | 2 |
| 梯度累积 | 8 |
| 学习率 | 2e-4 |
| 最大序列长度 | 1024 |
| 训练时长 | ~39分钟 |

### 4.5 训练数据格式

微调数据采用JSONL格式，每行一个训练样本：

```json
{
  "text": "指令: Walk forward, the path is blocked...\n感知信息: {...}\n轨迹信息: {...}\n决策输出: {...}"
}
```

### 4.6 动态LoRA切换机制

由于仅对**决策任务**进行微调，系统采用动态切换机制：

| 调用场景 | 是否使用LoRA | 说明 |
|----------|--------------|------|
| PerceptionAgent（视觉感知） | 不使用 | 保留原始VLM能力 |
| DecisionAgent（导航决策） | 使用 | 获得微调增强效果 |
| 其他Agent | 不使用 | 未微调，保持原有能力 |

**架构示意**：
```
vLLM Server
├── Base Model: Qwen3.5-9B (完整VLM能力)
├── LoRA Adapter: qlora_mixed/decision (~126MB)
└── 推理时动态选择是否启用LoRA
```

### 4.7 微调产出

| 产出文件 | 说明 |
|----------|------|
| adapter_model.safetensors | LoRA适配器权重（126MB） |
| train_metrics.json | 训练指标记录 |
| checkpoint-{N}/ | 各阶段checkpoint |

**训练最终Loss**: 0.5014
```

---

### 5. 总结

```markdown
## 5. 总结

本系统通过三个核心模块实现了VLN导航的智能化与应急能力扩展：

| 模块 | 技术亮点 |
|------|----------|
| 算法结构 | 自适应架构 + 多智能体协作 + 应急快速响应 |
| 应急数据集 | 基于R2R改造 + 三类场景模板 + 自动难度评估 |
| 模型微调 | QLoRA高效微调 + 动态LoRA切换机制 |

后续工作：大规模评估实验、性能指标验证、更多应急场景扩展。
```

---

## 二、PPT大纲

### 总览（共18页）

| 页码 | 标题 | 内容要点 | 视觉元素 |
|------|------|----------|----------|
| **P1** | 多智能体VLN导航系统 | 一句话定义 + 三大组成部分 | 系统架构总览图 |
| **P2-3** | 弱层-强层双层架构 | 架构对比 + 双层切换机制 | 金字塔架构图 |
| **P4-5** | 5级任务复杂度分类 | Type-0~Type-4表格 + 分类流程 | 渐变复杂度图示 |
| **P6-7** | 四类智能体 + 四种策略 | 智能体职责 + 策略适用表 | 智能体图标 + 策略流程图 |
| **P8** | 应急快速路径机制 | 正常模式 vs 应急模式对比 | 双路径对比流程图 |
| **P9** | 三大应急场景类型 | 场景定义 + 指令示例 | 场景示意图 |
| **P10-11** | 基于R2R的应急数据改造 | 构建流程 + 模板设计 | 指令转换示意 |
| **P12** | 数据集构成与划分 | 训练/验证/测试比例 + 样本结构 | 饼图 + JSON结构 |
| **P13** | QLoRA高效微调方法 | 三大优势 + 方法对比 | 显存对比柱状图 |
| **P14-15** | LoRA参数与训练配置 | 参数表 + 目标模块 | 模型结构标注图 |
| **P16** | 动态LoRA切换机制 | Agent调用对照 + 架构示意 | vLLM架构图 |
| **P17** | 微调产出文件 | 产出文件表 + 训练指标 | 文件目录结构 |
| **P18** | 总结与展望 | 系统特点回顾 + 后续工作 | 简洁回顾卡片 |

### 详细设计

**幻灯片1：项目简介**
- 标题：多智能体VLN导航系统
- 内容：一句话定义 + 三大组成部分卡片
- 视觉：系统架构总览图

**幻灯片2-3：分层架构**
- 标题：弱层-强层双层架构
- 内容：架构对比表格 + 双层切换示意图
- 视觉：金字塔图，底部弱层，顶部强层

**幻灯片4-5：任务分类**
- 标题：5级任务复杂度分类
- 内容：Type-0到Type-4表格 + 分类流程图
- 视觉：从简单到复杂的渐变图示

**幻灯片6-7：智能体与策略**
- 标题：四类智能体 + 四种策略
- 内容：智能体职责表 + 策略适用表
- 视觉：智能体图标阵列 + 策略流程图

**幻灯片8：应急响应**
- 标题：应急快速路径机制
- 内容：正常模式 vs 应急模式对比
- 视觉：两条路径的对比流程图

**幻灯片9：应急场景定义**
- 标题：三大应急场景类型
- 内容：场景类型表 + 典型指令示例
- 视觉：三种场景的示意图（路径阻断、动态障碍、紧急撤离）

**幻灯片10-11：数据集构建**
- 标题：基于R2R的应急数据改造
- 内容：构建流程图 + 模板设计示意
- 视觉：原始指令→应急指令的转换示意

**幻灯片12：数据集规模**
- 标题：数据集构成与划分
- 内容：训练/验证/测试划分表 + 样本结构示意
- 视觉：饼图展示数据集规模分布

**幻灯片13：QLoRA方法**
- 标题：QLoRA高效微调方法
- 内容：三大优势 + 与全量微调对比
- 视觉：显存占用对比柱状图

**幻灯片14-15：训练配置**
- 标题：LoRA参数与训练配置
- 内容：参数配置表 + 目标模块示意
- 视觉：模型结构图标注LoRA目标模块

**幻灯片16：动态切换机制**
- 标题：动态LoRA切换机制
- 内容：各Agent调用对照表 + 架构示意
- 视觉：vLLM Server架构图，标注LoRA切换点

**幻灯片17：微调产出**
- 标题：微调产出文件
- 内容：产出文件表 + 最终训练指标
- 视觉：文件目录结构示意

**幻灯片18：总结**
- 标题：总结与展望
- 内容：系统特点回顾 + 后续工作
- 视觉：简洁回顾卡片

---

## 三、设计决策记录

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 文档用途 | 项目展示/答辩汇报 | 用户指定 |
| 受众 | 混合受众（导师+同学） | 需兼顾深度和清晰度 |
| 内容比例 | 均衡分配（各35%、35%、20%） | 三部分重要性相当 |
| 呈现形式 | Markdown + PPT大纲 | 用户指定 |
| 实验结果 | 暂不包含 | 当前实验结果尚不完整 |
| 项目概述 | 简化版（无背景/问题/创新点） | 用户要求简化 |
| 结构方案 | 论文式结构 | 逻辑清晰，符合答辩习惯 |

---

*文档创建时间: 2026-04-10*